{
	"name": "jelly-party-join",
	"private": true,
	"version": "0.0.1",
	"scripts": {
		"dev": "npx -y serve public -l 3000",
		"build": "echo 'Static site - no build needed'",
		"preview": "npx -y serve public -l 3000"
	}
}
